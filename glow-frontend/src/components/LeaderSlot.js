import React, { Component } from 'react';

const LeaderSlot = (props) => {
  {
    return(
      <div>
        <h4>Username: {props.player}  Points: {props.points} </h4>
      </div>
    )
  }
}

export default LeaderSlot
